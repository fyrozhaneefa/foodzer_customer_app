import 'package:flutter/material.dart';
class SizedWidth extends StatelessWidget {
  const SizedWidth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 70);
  }
}
