import 'package:flutter/material.dart';
class SizedWidget extends StatelessWidget {
   SizedWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 30);
  }

}
