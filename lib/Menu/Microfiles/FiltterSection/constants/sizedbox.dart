import 'package:flutter/material.dart';

class Swidth extends StatelessWidget {
  const Swidth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 60);
  }
}
